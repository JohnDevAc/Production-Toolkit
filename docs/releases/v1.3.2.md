# Production Toolkit 1.3.2

Installation status now distinguishes Client and Server roles using durable component receipts and independent evidence. Malformed optional PC Agent metadata cannot hide valid Server evidence; unsupported role receipts are reported as unknown. Client-only PCs do not need local server components.

Verified cached installers remain usable offline. Downloads and recovery validate complete package contents, expected application identity and the required network sources. Resolume and PC Agent readiness use the suite's stable endpoint/job contracts.

Validation: 281 full regression/UI assertions passed before release preparation. The release workflow also tests installation, running-app upgrade, shortcuts and uninstall on its Windows runner.
